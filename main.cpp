#include <iostream>

using namespace std;

int sum(int first, int second){
    int x = first + second;
    return x;
}

int main() {

    int number;
    int value = sum(12, 13);
    int value2 = sum(-1, 0);
    return 0;
}
